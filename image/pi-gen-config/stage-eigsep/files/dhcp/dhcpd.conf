# EIGSEP private LAN DHCP config — installed at /etc/dhcp/dhcpd.conf by
# the image build. Only the Pi with `dhcp = true` in /boot/eigsep-role.conf
# enables isc-dhcp-server.service, so this file is inert on every other Pi.
#
# Authority: this repo (image/pi-gen-config/stage-eigsep/files/dhcp/).
# Migrate static reservations here when adding new hardware to the LAN.

option domain-name "eigsep.local";

default-lease-time 600;
max-lease-time 7200;

ddns-update-style none;

subnet 10.10.10.0 netmask 255.255.255.0 {
  interface eth0;
  range 10.10.10.12 10.10.10.255;
  option routers 10.10.10.1;
}

# Static reservations (snap1/snap2 SNAP boards).
host snap1 {
  hardware ethernet 00:08:cf:3d:13:03;
  fixed-address 10.10.10.236;
}

host snap2 {
  hardware ethernet 00:35:56:0e:17:01;
  fixed-address 10.10.10.13;
}
