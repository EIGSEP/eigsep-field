# Per-firmware local manifest. Mirrors the [firmware.rfsoc_bitstream]
# table in the top-level manifest.toml.

version = "2026-04-01"
asset   = "eigsep_vivaldi_2026-04-01.npz"
source  = "https://github.com/EIGSEP/eigsep_dac"
commit  = ""
sha256  = ""

# Notes:
#   The host programmer for this bitstream is the eigsep_dac wheel,
#   pinned in the top-level manifest.toml under [hardware.eigsep_dac].
#   Its tag must match this `commit`.
