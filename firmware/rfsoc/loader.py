"""Vendored from eigsep_dac at the commit pinned in manifest.toml.

Replaced wholesale on each bitstream bump. Do not modify in place — make
changes upstream in eigsep_dac and re-vendor.
"""
# TODO: populate from eigsep_dac at the pinned commit during the first real
# release bump. Stub raises so nothing silently no-ops if imported
# prematurely.

raise NotImplementedError(
    "firmware/rfsoc/loader.py is a stub until the first RFSoC bitstream "
    "release is cut. Re-vendor from eigsep_dac at the commit in "
    "manifest.toml's [firmware.rfsoc_bitstream]."
)
